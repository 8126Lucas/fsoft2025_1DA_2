#include <iostream>

/**
 * Esta é a função principal para a sua APLICAÇÃO FINAL, não para os testes.
 * Os testes correm de forma independente através do alvo 'runTests'.
*/
int main(int argc, char **argv) {
    // Aqui começaria o seu programa FleetManager
    std::cout << "Bem-vindo ao FleetManager!" << std::endl;
    std::cout << "Para executar os testes, use o alvo 'runTests' no seu IDE." << std::endl;

    // No futuro, aqui poderá chamar a sua view principal, por exemplo:
    // View mainView;
    // mainView.showMainMenu();

    return 0;
}